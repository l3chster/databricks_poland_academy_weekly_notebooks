[project]
name = "crime_info_pipeline"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [ ]

[dependency-groups]
dev = [    
    "ruff",
    "databricks-dlt",
    "databricks-connect>=16.4,<16.5"    
]

[tool.ruff]
line-length = 120
builtins = ["spark"]
